{
  "name": "pigeonai-notification-lambda",
  "version": "1.0.0",
  "description": "AWS Lambda function for Pigeon AI push notifications",
  "main": "index.js",
  "scripts": {
    "build": "zip -r function.zip index.js node_modules package.json",
    "test": "node test.js"
  },
  "dependencies": {
    "@opensearch-project/opensearch": "^3.5.1",
    "axios": "^1.6.0",
    "firebase-admin": "^12.0.0"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
